// Importing the 'request' module from the 'postman-request' library
// We need this module to make HTTP requests to our Express server

const request = require("postman-request");

// Importing the URL of our Express server
// We need this to specify the base URL for our requests

const SERVER_URL = "http://localhost:3000"; // Replace with the actual URL of your server
